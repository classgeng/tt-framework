package ${mapperPath};

import ${modelPath}.${modelName};
import com.baomidou.mybatisplus.core.mapper.BaseMapper;

/**
 * @author ${author}
 * @description ${tableName} Mapper接口 ${mapperDescription}
 * @project ${projectName}
 * @date ${dateTime}
 */
public interface ${mapperName} extends BaseMapper${"<"}${modelName}${">"} {
}
